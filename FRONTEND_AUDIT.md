# VerexaHQ Frontend Audit

Audited on branch `frontend-audit-and-completion` (based on the tip of `improve-system-templates-form-experience`, which includes the merged-but-unreleased Client-First System Template and Product Polish work).

**Overall assessment:** this is a mature, largely complete application — not a demo or a collection of disconnected screens. All 41 routes build cleanly, `npm run typecheck` and `npm run lint` are clean, every page checked reads from real Supabase tables (no hardcoded mock data, no `lorem ipsum`, no "coming soon" stub pages), and the 6 highest-traffic pages all have loading, empty, and error states. The issues below are real but incremental — mostly navigation discoverability, a few missing pieces of global UI infrastructure (toast feedback, accessible confirm dialogs), `any`-type cleanup, and one significant architectural question for the backend/product owner (see Issue 2).

| # | Issue | Location | Severity | Recommended fix | Status |
|---|---|---|---|---|---|
| 1 | `/pipeline` and `/services` are fully built, real, Supabase-backed pages (kanban pipeline view and services list, both hitting the same `services`/`pipeline_stages` tables `/work` uses) with **zero links pointing to them anywhere in the app** — reachable only by typing the URL. | `app/(app)/pipeline/page.tsx`, `app/(app)/services/page.tsx` | High | Add discoverable links from `/work`, since they cover the same domain. | **Fixed** — added "Pipeline view" and "All services" links to the `/work` page header (`app/(app)/work/page.tsx`). |
| 2 | `/bookkeeping`, `/payroll`, and `/tax` (list + detail pages, 300–780 lines each, fully Supabase-backed) are **completely unreachable from any UI** — no nav entry, no link from `/work`, no link from client profiles. They read from dedicated tables (`bookkeeping_engagements`, `payroll_clients`, `tax_returns`) that are separate from the unified `services`/engagement-workspace model `/work` and the client profile use. Verified live against confirmed production (`euxfopzgdmlmgcmmjvic`): `tax_returns.engagement_id` and `payroll_clients.engagement_id`/`.service_id` already provide a real FK path into the unified `engagements`/`services` model; `bookkeeping_engagements` has neither column and has no path at all. All four tables are currently empty in production, so this is a schema-capability finding, not a data-migration one. | `app/(app)/bookkeeping/*`, `app/(app)/payroll/*`, `app/(app)/tax/*` | High | **Needs a product decision, not a frontend patch.** `/tax` and `/payroll` could become specialized Service Workspace views using existing columns; `/bookkeeping` needs a new linking column first (a real migration, out of scope for this pass). See `FRONTEND_BACKEND_GAPS.md` #1 for the full column-by-column finding. | **Documented, not changed** — deliberately left alone pending a decision, since either "fix" (delete vs. promote vs. build specialized views) would be a real product/architecture call, not a safe frontend polish. |
| 3 | `/deadlines` has only one path in: a "Upcoming deadlines" widget link on the dashboard. Not in primary nav. | `app/(app)/dashboard/page.tsx:161`, `app/(app)/layout.tsx` `PRD_NAV` | Low | Acceptable as designed — a dashboard drill-in link is a normal SaaS pattern, and the page is fully functional once reached. | No change — not a defect. |
| 4 | No toast/success-feedback system exists anywhere in the app. Confirmation dialogs use the native browser `window.confirm()` (20 sites across 17 files) instead of an in-app, on-brand, accessible dialog. The task spec explicitly calls for both "Toast feedback" and "Confirmation dialogs" as global shell requirements. | App-wide | Medium | Build a lightweight `ToastProvider`/`useToast()` and a reusable `ConfirmDialog` component; wire them into the shell and every destructive action. | **Fully fixed** — see `components/Toast.tsx` and `components/ConfirmDialog.tsx`, wired into the app shell and every `window.confirm()` site app-wide (billing line items, engagements, tax returns/estimates, payroll runs/items/employees/filings/tax deposits/clients, recurring invoices, financial accounts, transactions, periods, deadlines, client delete). Zero `window.confirm()` calls remain in the codebase. |
| 5 | Dashboard metric tiles show a bare `"—"` while loading instead of a skeleton. | `app/(app)/dashboard/page.tsx:137-140` | Low | Replace with a skeleton pulse matching the metric tile shape. | **Fixed**. |
| 6 | `invoice: any` in the dashboard's outstanding-balance reducer, and 85 other avoidable `any` usages app-wide (top offenders: `app/(app)/work/page.tsx` ×15, `components/ClientModal.tsx` ×14, `app/(app)/work/[engagementId]/page.tsx` ×11, `app/(app)/clients/[id]/page.tsx` ×10). | App-wide, see full list in this audit's companion grep | Low–Medium | Type against real `lib/types.ts` shapes; most are `useState<any[]>` for raw Supabase rows or `catch (e: any)`, both mechanically fixable. | **Further fixed this pass** — `app/(app)/work/page.tsx` (15), `components/ClientModal.tsx` (14), and `app/(app)/work/[engagementId]/page.tsx` (11) are now fully typed against real `.select(...)` column shapes, with `as unknown as X` used where Supabase's untyped client infers embedded to-one relations as arrays. ~33 lower-traffic `any` usages remain (`app/(app)/clients/[id]/page.tsx` and others not touched this pass) and are listed as follow-up work, not silently dropped. |
| 7 | Dashboard, Tasks, and Work pages inline their own raw error strings instead of using the shared `friendlyError()` helper that `clients`, `documents`, `pipeline`, `deadlines`, `bookkeeping`, `payroll`, and `tax` list pages already use consistently. | `app/(app)/dashboard/page.tsx:86`, `app/(app)/tasks/page.tsx`, `app/(app)/work/page.tsx` | Low | Route through `friendlyError()` for consistency with the rest of the app. | **Fixed**. |
| 8 | `communication_messages` table is read in exactly one place (`app/(app)/clients/[id]/page.tsx:371`), feeding a real "Communication" tab and "Recent Communication" summary card, both already correctly built with proper empty states. Verified live against confirmed production (`euxfopzgdmlmgcmmjvic`): the table is real, has full CRUD RLS for `authenticated`, and its columns (`direction`, `channel`, `message_status`, `from_address`, `to_address`, `provider_name`, etc.) are exactly an email/SMS delivery log — correctly distinct from `secure_message_threads`/`secure_messages` (engagement messaging) and `portal_conversations`/`portal_messages` (client portal messaging). It has 0 rows because nothing in the codebase writes to it yet (`lib/notify.ts`'s send functions don't log to it) — not because it's the wrong table. | `app/(app)/clients/[id]/page.tsx:371` | Low | No fix needed to the read itself — it's already correct. If a communication-history feature is wanted, wire `lib/notify.ts`'s send functions to insert a row per send; no new schema required. | **Verified correct, not a bug** — see `FRONTEND_BACKEND_GAPS.md` #2 for the full finding. |
| 9 | `app/(app)/help/page.tsx` is static, hardcoded help copy with no Supabase calls (52 lines). | `app/(app)/help/page.tsx` | Informational | This is legitimate static content (a help/FAQ page), not a stub — flagged only because the audit's "no hardcoded content" check would otherwise miss explaining why it's an exception. | No change — working as intended. |
| 10 | `components/ActivateServiceModal.tsx` has one explicitly-disabled, clearly-labeled "Workflow template builder coming soon" control inside an otherwise fully functional modal. | `components/ActivateServiceModal.tsx:617,621` | Informational | Already correctly disabled and labeled per the task's own rule ("clearly marked as unavailable") — no fix needed. | No change — working as intended. |
| 11 | `react-hooks/exhaustive-deps` lint warnings on 8 pages/components (all pre-existing, not introduced by recent work) — mostly a `load` callback intentionally excluded from its own `useEffect` dependency array. | `app/(app)/billing/[id]/page.tsx:109`, `bookkeeping/[id]/page.tsx:82`, `bookkeeping/periods/[periodId]/page.tsx:323`, `clients/[id]/page.tsx:425`, `payroll/[id]/page.tsx:93`, `payroll/runs/[runId]/page.tsx:56`, `pipeline/page.tsx:91`, `tax/[id]/page.tsx:60`, `tax/organizers/[id]/page.tsx:121`, `work/[engagementId]/page.tsx:158`, `components/ActivateServiceModal.tsx:221,239`, `components/NewServiceModal.tsx:76` | Low | Each is the common, usually-safe "stable load function excluded to avoid re-fetch loops" pattern — verify case-by-case before touching, since a blind fix risks introducing fetch loops. | **Left as-is this pass** — real but out of scope for a safe polish change; flagged as follow-up. |
| 12 | `components/BrandCenter.tsx` uses a plain `<img>` instead of `next/image` for the logo preview. | `components/BrandCenter.tsx:250` | Low | Cosmetic performance warning only, not a defect. | Not fixed this pass — low priority. |
| 13 | *(Resolved.)* A project-identity question was raised mid-engagement about which Supabase project is production. It's now confirmed, via live verification against both candidate projects, that `euxfopzgdmlmgcmmjvic` (the project this codebase's `.env.local.example` has pointed at the whole time) is correct: all 59 tables, 3 views, 29 RPCs, and the 1 storage bucket the frontend references exist there, with matching column shapes and RPC signatures spot-checked on the highest-traffic resources. | App-wide (every Supabase call) | Informational | No action needed — the frontend's Supabase target was correct throughout. | **Resolved** — see `FRONTEND_BACKEND_GAPS.md` "Project-identity correction" for the full verification. |

## Baseline QC results (before this pass's fixes)

- `npm ci` — clean
- `npm run typecheck` — clean, 0 errors
- `npm run lint` — 0 errors, 14 pre-existing warnings (all `react-hooks/exhaustive-deps` + 1 `no-img-element`, see Issue 11/12)
- `npm run build` — clean, all 41 routes compile

## QC results after this continuation pass (any-type cleanup + full window.confirm() conversion)

- `npm run typecheck` — clean, 0 errors
- `npm run lint` — 0 errors, same 14 pre-existing warnings, no new warnings introduced
- `npm run build` — clean, all 41 routes compile

See `FRONTEND_COMPLETION_REPORT.md` for the full list of files changed.
