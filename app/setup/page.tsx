export default function SetupPage(){return <main className="min-h-screen bg-paper px-4 py-12 grid place-items-center"><section className="w-full max-w-xl bg-white border border-line rounded-sm p-6 sm:p-8"><h1 className="text-2xl font-bold text-ink">Connect Supabase in Vercel</h1><p className="mt-3 text-sm text-muted">Add the following environment variables under Vercel → Project → Settings → Environment Variables, then redeploy.</p><pre className="mt-5 p-4 bg-paper border border-line text-xs overflow-x-auto">NEXT_PUBLIC_SUPABASE_URL{`
`}NEXT_PUBLIC_SUPABASE_ANON_KEY</pre><p className="mt-4 text-xs text-muted">Apply both values to Production, Preview, and Development. Never place the service-role key in a NEXT_PUBLIC variable.</p></section></main>}
